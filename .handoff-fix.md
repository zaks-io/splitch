# SPL-344 file-size guard fix

## Changes

- Moved the new Exposure identity transition and Exposure Ticket refresh-window coverage from `apps/evaluation-api/src/evaluate-all.test.ts` into `apps/evaluation-api/src/evaluate-all-exposure-identity.test.ts` without removing or weakening assertions.
- Moved the Evaluate All ETag material, canonicalization, hashing, and `If-None-Match` helpers from `apps/evaluation-api/src/evaluate-all.ts` into `apps/evaluation-api/src/evaluate-all-exposure-identity.ts`.

## Files changed

- `apps/evaluation-api/src/evaluate-all.test.ts`
- `apps/evaluation-api/src/evaluate-all.ts`
- `apps/evaluation-api/src/evaluate-all-exposure-identity.test.ts` (new)
- `apps/evaluation-api/src/evaluate-all-exposure-identity.ts` (new)

## File-size results

- `apps/evaluation-api/src/evaluate-all.test.ts`: 310 code lines, down from the 325-line baseline.
- `apps/evaluation-api/src/evaluate-all.ts`: 234 code lines.
- `apps/evaluation-api/src/evaluate-all-exposure-identity.test.ts`: 159 code lines.
- `apps/evaluation-api/src/evaluate-all-exposure-identity.ts`: 75 code lines.

`git add -A` could not update the real worktree index because the sandbox cannot create `/Users/isaacsuttell/src/splitch/.git/worktrees/wt-spl-344/index.lock`. Consequently, `node scripts/check-file-size.mjs` could not be run against the updated staged contents. No alternate index or gate bypass was used.

## Checks

- `../../node_modules/.bin/vitest run --passWithNoTests`: passed, 45 files and 276 tests.
- `../../node_modules/.bin/tsc -p tsconfig.json --noEmit`: passed.
- Biome check for the four split files: passed.
- `git diff --check`: passed.
- `pnpm --filter @splitch/evaluation-api test`: blocked before the test script because pnpm's dependency status check reran install and root `prepare` could not replace the read-only repository Lefthook files.
- `pnpm --filter @splitch/evaluation-api typecheck`: blocked by the same dependency status check before the package script ran.

No commit or push was performed.
