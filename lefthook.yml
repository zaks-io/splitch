pre-commit:
  parallel: false
  commands:
    file-size:
      run: node scripts/check-file-size.mjs
    verify-commit:
      run: CI=true pnpm verify:commit

pre-push:
  parallel: false
  commands:
    verify-push:
      run: CI=true pnpm verify:push
