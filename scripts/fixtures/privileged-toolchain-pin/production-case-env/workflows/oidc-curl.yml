# Negative fixture: GitHub environment names are case-insensitive.
# `Production` must classify as privileged.
name: production-case-env-mutable-installer
on: push
jobs:
  deploy:
    runs-on: ubuntu-24.04
    environment: Production
    steps:
      - name: Install
        run: curl -LsSf https://example.test/install.sh | sh
